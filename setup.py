import setuptools

setuptools.setup(
    name="CoPyCAT",
    version="0.0.1",
    description="A python package for measuring verbal mirroring in dyadic conversations",
    packages=setuptools.find_packages(),
    python_requires='>=3.9',
)